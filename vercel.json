{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "npm run update-browserslist && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install"
}
